services:
  app:
    build:
      context: .
      target: development
    working_dir: /workspace
    volumes:
      - .:/workspace

  bot:
    build:
      context: .
      target: runtime
    env_file: .env
    restart: unless-stopped
