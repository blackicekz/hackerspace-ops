name: deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Provide operator-managed .env
        run: cp /opt/github-actions-runner/secrets/hackerspace-ops.env .env

      - name: Build bot image
        run: docker compose build bot

      - name: Deploy bot
        run: docker compose up -d bot
